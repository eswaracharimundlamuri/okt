node {
    stage('Build') {
        // Compile source code
        sh 'javac Main.java'
    }

    stage('Test') {
        // Run tests
        sh 'java TestSuite'
    }

    stage('Deploy') {
        // Deploy to production
        sh 'deploy.sh'
    }
}
